import React from 'react';
import { View, Text } from 'react-native';

import { Heading } from '../../../components';

const Register = ({ navigation }) => {
  return (
    <View>
      <Heading>Register Screen Placeholder</Heading>
    </View>
  );
};

export default Register;
